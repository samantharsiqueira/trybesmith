/* import express from 'express';
import loginController from '../controllers/loginController';
import { ROUTE } from '../utils/index';

const loginRouter = express.Router();

loginRouter.post(ROUTE.LOGIN, loginController.login); */